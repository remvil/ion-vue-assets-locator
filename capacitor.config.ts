import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-vue-assets-locator',
  webDir: 'dist'
};

export default config;
